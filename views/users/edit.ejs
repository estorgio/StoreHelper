<% include ../partials/header %>

<div class="container">

    <div class="row">
        <div class="col-md-12">
            <h1><i class="fa fa-pencil-square-o" aria-hidden="true"></i> Edit Account Info</h1>
            <hr>
        </div>
    </div>

    <div class="row">
        <div class="col-md-5">
            <form action="/users/<%= user._id %>/?_method=PUT" method="post">
                <div class="form-group">
                    <label for="firstname">First Name</label>
                    <input type="text" name="user[firstname]" id="firstname" class="form-control" value="<%= user.firstname %>">
                </div>
                <div class="form-group">
                    <label for="lastname">Last Name</label>
                    <input type="text" name="user[lastname]" id="lastname" class="form-control" value="<%= user.lastname %>" >
                </div>
                <div class="form-group">
                    <label for="username">Username</label>
                    <input type="text" name="user[username]" id="username" class="form-control" value="<%= user.username %>" >
                </div>
                <div class="form-group">
                    <label for="accountType">Account Type</label>
                    <select name="user[type]" id="accountType" class="form-control" >
                        <option value="unspecified" <%= user.type === 'cashier' ? 'selected' : '' %>>Select account type</option>
                        <option value="cashier" <%= user.type === 'cashier' ? 'selected' : '' %>>Cashier</option>
                        <option value="manager" <%= user.type === 'cashier' ? 'selected' : '' %>>Manager</option>
                        <option value="admin" <%= user.type === 'cashier' ? 'selected' : '' %>>Administrator</option>
                    </select>
                </div>
                <div class="pull-right">
                    <button type="submit" class="btn btn-primary">Save</button>
                </div>
                <a href="/users/<%= user._id %>" class="btn btn-default"><i class="fa fa-angle-double-left" aria-hidden="true"></i> Back</a>
            </form>
        </div>
    </div>

</div>

<% include ../partials/footer %>